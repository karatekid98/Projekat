import { TranslateLabelsPipe } from './translate-labels.pipe';

describe('TranslateLabelsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateLabelsPipe();
    expect(pipe).toBeTruthy();
  });
});
